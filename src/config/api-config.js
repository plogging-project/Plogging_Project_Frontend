// let backendHost;
//
// const hostname = window && window.location && window.location.hostname;
// if (hostname === 'localhost') {
//   backendHost = 'https://jub.sionms.co.kr';
// }
// export const API_BASE_URL = `${backendHost}`;
